<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/output.css">
    <title>Membership Form - Advice Board</title>
</head>
<body class="bg-background min-h-screen flex items-center justify-center">
    <div class="bg-text/85 rounded-lg shadow-lg p-8 w-full max-w-md animate-scale-in">
        <h1 class="text-3xl font-bold text-center mb-6 bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent">Membership Form</h1>
        
        <% if (locals.message) { %>
            <div class="mb-6 p-4 rounded-md <%= message.type === 'error' ? 'bg-accent/20 text-accent' : 'bg-primary/20 text-primary' %>">
                <%= message.text %>
            </div>
        <% } %>

        <form action="/membership" method="POST" class="space-y-6">
            <div>
                <label for="secret" class="block text-sm font-medium text-primary mb-2">Secret Code:</label>
                <input 
                    type="text" 
                    id="secret" 
                    name="secret" 
                    required
                    class="w-full p-3 rounded-md border border-secondary/50 bg-background text-text placeholder-text/50 focus:outline-none focus:ring-2 focus:ring-primary transition-all duration-300 ease-in-out"
                    placeholder="Enter your secret code"
                />
            </div>
            <button 
                type="submit" 
                class="w-full bg-primary text-background py-3 rounded-md font-bold hover:bg-accent transition-all duration-300 ease-in-out hover:scale-105"
            >
                Submit
            </button>
        </form>
    </div>
</body>
</html>

